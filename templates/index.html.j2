<!doctype html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="__internal_only_easywind_css_file__.css">
  </head>
  <body>

  <div class="container px-2">
    {% for link in links %}
      <a href="{{ link }}" class="text-blue-500">{{ link }}</a>
    {% endfor %}
  </div>

  </body>
</html>
